# Synx

Synx 是一种语法定义语言：用声明式规则描述词法/语法，并据此类规则自动生成目标语言的解析器。

# 开发状态

**本项目目前处于未完成的初期开发阶段，不建议当前阶段使用。**

# 项目路径

- **`synx.synx`**：Synx 自身的语法定义（用 Synx 规则描述 Synx 语言）。
- **`synx-ts/`**：解析器实现（TypeScript），负责按语法规则解析输入并产出 AST 等结果。
- **`synx-vscode/`**：VS Code 语言插件（语法高亮、语言服务等与编辑器集成相关能力）。

